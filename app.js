document.addEventListener('DOMContentLoaded', () => {
  console.log('App loaded, data available:', artistsYearData, labelPricing);
  // Rendering logic here (labels, artists, modals, charts, etc.)
});
